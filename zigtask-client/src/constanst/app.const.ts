export const SSOCOOKIES = {
  access: "access_zigtask",
};

export enum ESuccessCodes  {
  SUCCESS = 200,
}

export enum EErrorCodes {
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  INTERNAL_SERVER_ERROR = 500,
}

